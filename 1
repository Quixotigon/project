def calculate_area(shape, length, width=None, radius=None):
if shape == "rectangle":
return length * width
elif shape == "circle":
return 3.14 * radius ** 2
else:
return "Invalid shape"

print(calculate_area("triangle", 5, 10)) # Output: Invalid shape
